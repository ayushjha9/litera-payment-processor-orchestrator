<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <ResourcePreloader />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["_content/Orchestrator.Ui.Components/components.css"]" />
    <link rel="stylesheet" href="@Assets["Orchestrator.Ui.styles.css"]" />
    <ImportMap />
    <HeadOutlet />
</head>

<body>
    @* Interactive for the whole app, so the per-circuit session identity is shared across
       navigation rather than being rebuilt per page. *@
    <Routes @rendermode="InteractiveServer" />
    <ReconnectModal />
    <script src="@Assets["_framework/blazor.web.js"]"></script>
</body>

</html>
